/**
 * Keeps track of infomation that will be used for displaying
 * elements in the view.
 */
export interface DisplayInfo {
    className: string;
    iconClassName: string;
    displayName: string;
}
